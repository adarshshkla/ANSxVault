services:
  - type: web
    name: ansx-sovereign-mesh
    env: python
    buildCommand: pip install -r relay_requirements.txt
    startCommand: uvicorn relay_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DB_PATH
        value: /opt/render/project/src/ansx_mesh.db
